#ifndef MAPLABO
#define MAPLABO

#include "dijkstra.h"

/**
 * \brief Fill the node map according to the path metrics
 * \param Map :Array of nodes describing the map
 * \param MapSize :Number of nodes in Map
*/
void InitMap(NodeStruct *Map, int MapSize);

#endif
